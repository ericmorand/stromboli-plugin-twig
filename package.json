{
  "name": "stromboli-plugin-twig",
  "version": "0.17.1",
  "description": "",
  "main": "src/plugin.js",
  "scripts": {
    "pretest": "npm prune && npm update",
    "test": "tap test/*.js --cov --reporter=spec --100"
  },
  "author": "Eric MORAND",
  "dependencies": {
    "fs-extra": "^2.0.0",
    "module-deps": "^4.1.1",
    "node-hook": "^0.4.0",
    "promise": "^7.1.1",
    "require-uncached": "^1.0.3",
    "twig-deps": "^1.0.2"
  },
  "repository": "ericmorand/stromboli-plugin-twig",
  "license": "Apache-2.0",
  "devDependencies": {
    "tap": "^7.1.2",
    "twig": "^1.10.0"
  },
  "peerDependencies": {
    "twig": "^0.8.0"
  }
}
